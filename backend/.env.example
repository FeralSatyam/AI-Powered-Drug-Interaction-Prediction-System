# ---- HTTP server ----
PORT=5000

# ---- React client (CORS) ----
# Comma-separated list of allowed origins for the browser client.
CLIENT_ORIGIN=http://localhost:3000,http://localhost:5173

# ---- Python FastAPI microservice ----
ML_SERVICE_URL=http://localhost:8000
# Timeout (ms) for requests to the microservice.
ML_SERVICE_TIMEOUT=30000

# ---- PostgreSQL ----
# Either provide a full connection string...
DATABASE_URL=postgres://postgres:postgres@localhost:5432/drug_interactions
# ...or the individual parts (used only if DATABASE_URL is empty).
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=drug_interactions

# Set to "true" to see SQL logging in the console.
DB_LOGGING=false
